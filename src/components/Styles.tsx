import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({

    weatherInput : {
        display: 'flex',
        flexWrap : 'wrap',
        justifyContent : 'center',
        marginTop : '20%',
        borderRadius : 3
    }

}));